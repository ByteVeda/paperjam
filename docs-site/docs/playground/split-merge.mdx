---
title: Split & Merge
description: Split PDFs by page ranges or merge multiple PDFs
---

# Split & Merge

Split PDFs by page ranges or merge multiple PDFs into one. All processing happens in your browser.

import BrowserOnly from '@docusaurus/BrowserOnly';

<BrowserOnly>
  {() => {
    const SplitMerge = require('@site/src/components/playground/SplitMerge').default;
    return <SplitMerge />;
  }}
</BrowserOnly>
